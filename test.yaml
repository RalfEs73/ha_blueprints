blueprint:
  name: Test
  description: Test, Test, Test, Test, Test, Test, Test, Test, Test
  domain: automation
  author: RalfEs73
  source_url: https://raw.githubusercontent.com/RalfEs73/ha_blueprints/refs/heads/main/test.yaml
  input:
    boolean_entity:
      name: Schalter Sensor
      selector:
        entity:
          filter:
            domain:
            - binary_sensor
          multiple: false
