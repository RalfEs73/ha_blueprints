blueprint:
  name: Test
  description: Test, Test, Test, Test, Test, Test, Test, Test, Test
  domain: automation
  author: RalfEs73
  input:
    motion_entity:
      name: Motion Sensor
      selector:
        entity:
          filter:
            - device_class: occupancy
              domain: binary_sensor
            - device_class: motion
              domain: binary_sensor